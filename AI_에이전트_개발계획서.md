# AI 에이전트 개발계획서

> 유니버스(Univ-Us) · AI 파트
> 작성 2026-08-13 · 전액 무료 · 자체 학습 기반

---

## 1. 개발 방향

### 1.1 기본 전제

| 항목 | 결정 |
|---|---|
| 비용 | **전액 무료.** 유료 API 미사용 |
| 모델 | 오픈 웨이트 모델을 **직접 파인튜닝** |
| 학습 환경 | 무료 클라우드 노트북 (주간 GPU 할당) |
| 서빙 환경 | 학교 서버 GPU 슬라이스 (가용 9.5GiB) |
| 프레임워크 | 슬롯 기반 추상화 유지 — 모델명을 코드에 쓰지 않음 |

### 1.2 왜 파인튜닝인가

캡스톤 과제이므로 **모델을 직접 학습시킨 결과**가 산출물이어야 한다. 상용 API 호출만으로는 기술 습득이 증명되지 않는다.

동시에 이 선택은 실용적 근거도 갖는다.

| 근거 | 설명 |
|---|---|
| 비용 | 대량 문서 반복 처리 시 API 단가가 누적. 로컬은 0 |
| 데이터 | 한국어 강의계획서 추출은 공개 데이터가 없어 어차피 자체 구축 필요 |
| 기여 | 없는 데이터셋을 만드는 것 자체가 결과보고서 산출물 |
| 제약의 명분화 | 대형 모델을 소형으로 압축해 9.5GiB에 올린 것이 성과 서사가 됨 |

### 1.3 슬롯 구조 유지

모델을 확정하지 않고 **슬롯 이름으로만 참조**하는 구조를 그대로 간다. 무료 전환이 이 구조를 깨지 않는다.

| core 완료 기준 | 무료 충족 방법 |
|---|---|
| 서로 다른 프로바이더 2종으로 동일 호출 성공 | 로컬 추론 서버 2종 (양쪽 다 OpenAI 호환 규격) |

설정 파일만 바꿔 로컬 ↔ 로컬 ↔ (필요 시 외부) 전환이 가능하다. **코드 변경 없이 방향 전환이 가능하다.**

---

## 2. 파이프라인 구성

AI 파트의 산출물은 기능 11개가 아니라 **파이프라인 4종**이다. 같은 파이프라인에 걸린 기능은 엔진을 공유한다.

| 구분 | 파이프라인 | 담당 기능 | 입력 → 출력 |
|---|---|---|---|
| **P1** | 구조화 추출 | F1 F6 F11a F13 | 비정형 문서 → 고정 스키마 + 출처 문장 + 신뢰도 |
| **P2** | 임베딩 · 검색 | F4 F12 | 청크 → 색인 → 유사 검색 결과 |
| **P3** | 대화 오케스트레이션 | F9 | 자연어 → 의도 · 도구 · 인자 → 자연어 응답 |
| **P4** | 자연어 생성 | F10 F11b | 구조화 데이터 → 문장 |

**P1이 전체 공수의 절반 이상이다.** F1·F13·F11a는 추출 대상만 다를 뿐 엔진이 동일하고 스키마만 교체된다.

### 2.1 파이프라인별 필수 제약

| 파이프라인 | 제약 |
|---|---|
| P1 | 출처 문장 없는 추출 결과는 반환하지 않는다. 신뢰도는 값만 붙여 넘기고 등록 판단은 백엔드 게이트가 한다 |
| P2 | 청크 분할 시점에 출처(파일·페이지)를 메타데이터로 저장한다. 이 시점을 놓치면 이후 복구 불가 |
| P3 | 읽기/쓰기 분기를 AI가 판단하지 않는다. 도구 정의 시점에 부여된 속성으로 결정한다 |
| P4 | 브리핑에 무엇을 담을지는 백엔드 집계가 정한다. AI는 문장화만 담당한다 |

### 2.2 AI가 담당하지 않는 것

| 작업 | 담당 | 이유 |
|---|---|---|
| 졸업요건 계산 (F2) | 백엔드 | 틀리면 기능 자체가 실패 |
| 결석 한도 판정 (F3) | 백엔드 | 규정 기반 임계치 |
| 우선순위 점수 (F7) | 백엔드 | 가중 점수식 |
| 학습 블록 배치 (F8) | 백엔드 | 제약 조건 탐색 |
| 시간표 제약 탐색 (F14) | 백엔드 | 결정적 계산 |
| **장학 자격 충족 판정 (F11)** | 백엔드 | AI는 요건을 읽어내는 것까지만 |

**판단 기준 한 줄 — 숫자를 만들어내는 일은 AI가 하지 않는다. AI는 텍스트를 읽거나 쓰는 일만 한다.**

> ⚠️ 계획서 내부에 이 부분이 상충하는 서술이 있다. 4.3 나 표에서는 F11~F13의 "공지 매칭 판단"을 주 LLM에 배정했으나, 4.3 다 ⑤ 파이프라인에서는 "프로필과 **규칙 매칭**"으로 기술되어 있다. **후자가 맞다.** 학점·학년 비교를 LLM에 맡기면 결정적이지 않아 오매칭률 10% 지표를 측정할 수 없다. 문서 정리 필요.

---

## 3. 개발 순서

### 3.1 F9를 먼저 하는 이유

| 기능 | 학습 데이터 | 현재 가용 |
|---|---|---|
| **F9 자연어 질의** | 한국어 function calling 데이터 | ✅ 공개 데이터셋 존재 |
| P1 문서 추출 | 한국어 강의계획서 + 정답 스키마 | ❌ 공개 없음, 자체 구축 필요 |

F9는 **지금 학습이 가능한 유일한 기능**이다. 여기서 학습 파이프라인을 완성해두면, 강의계획서 확보 후 데이터만 교체해 P1으로 이행할 수 있다.

순서를 뒤집으면 데이터를 모으는 동안 학습 환경 구축이 멈추고, 데이터가 모인 뒤 환경 문제로 다시 시간을 쓴다. **환경과 데이터를 병렬 진행하는 것이 핵심이다.**

### 3.2 단계

| 단계 | 기간 | 내용 | 산출물 |
|---|---|---|---|
| **A0** | 1~2일 | 학습 환경 구축 · 계정 · 인증 | 환경 확인 로그 |
| **A1** | 3일 | 최소 모델로 학습 루프 검증 | 동작하는 학습 노트북 |
| **A2** | 1~2주 | **F9 학습** (공개 데이터) | 어댑터 + 평가 수치 |
| **A3** | 병행 | 데이터 확보 (수집 계획서 소관) | 강의계획서 · 공개 데이터 |
| **A4** | 9~10월 | **P1 학습** (증류 방식) | 추출 전용 모델 |
| **A5** | 10~11월 | P2 색인 · 검색 구축 | RAG 동작 |
| **A6** | 11월 | P4 생성 · F11 통합 | 브리핑 · 신청서 초안 |

**A1을 건너뛰지 않는다.** 최소 모델로 3일 검증하는 비용이, A2에서 같은 문제를 만나는 비용보다 훨씬 싸다.

---

## 4. 모델 구성

### 4.1 2계층 구조

| 역할 | 위치 | 용도 |
|---|---|---|
| **Teacher** | 무료 클라우드 (약 32GB) | 학습 데이터 라벨 생성 |
| **Student** | 학교 서버 (9.5GiB) | 실제 서비스 추론 |

대형 Teacher로 라벨을 만들고 소형 Student에 증류하는 구조다. 양쪽 모두 오픈 웨이트이므로 **비용이 발생하지 않는다.**

### 4.2 선정 기준

| # | 요구 능력 | 근거 |
|---|---|---|
| 1 | 한국어 문서 이해 | 강의계획서·공지가 전부 한국어. 날짜 표기가 제각각 |
| 2 | 구조화 출력 | 스키마 검증을 통과해야 함. 실패 시 재시도 비용 누적 |
| 3 | 도구 호출 | 대화형 에이전트가 도구를 고르고 인자를 채워야 함 |
| 4 | 컨텍스트 길이 | 강의계획서 전문 + 대화 이력 수용 |

컨텍스트는 **실측 기준으로 판단한다.** 강의계획서 1건 평균 약 3,200토큰(최장 7,800), 대화 이력 20턴 약 4,500토큰. 합산 약 16,000토큰 규모다. "길수록 좋다"가 아니라 "우리 문서에 충분한가"가 기준이다.

### 4.3 학습 대상 선택

**Instruct가 아닌 Base 모델을 사용한다.**

| 이유 | 설명 |
|---|---|
| 목적 부합 | Base는 파인튜닝·in-context 실험용으로 배포됨 |
| 기술적 이점 | 제어 토큰이 공식 템플릿과 함께 학습되어 어댑터 학습 시 임베딩 재학습 불필요 |
| 성과 증명 | "우리가 학습시켰다"를 보이기에 명확 |

### 4.4 환경별 배치

| 환경 | 가용 자원 | 담당 |
|---|---|---|
| 무료 클라우드 노트북 | 약 32GB (16GB × 2) | **학습 전용** |
| 학교 서버 슬라이스 | 9.5GiB | 추론 서빙 · 색인 |

⚠️ **학습을 학교 서버에서 하지 않는다.** 가용 메모리가 3배 이상 차이나며, 서버는 수집·서빙으로 상시 점유되어야 한다.

⚠️ 클라우드 노트북의 "32GB"는 **통합 메모리가 아니다.** 16GB 두 장이며, 소형 모델 학습에는 한 장이면 충분하다. 분할은 Teacher 추론 단계에서만 필요하다.

⚠️ 무료 GPU 세대에 따라 **일부 연산 정밀도가 지원되지 않는다.** 환경 확인 단계에서 지원 여부를 먼저 확인하고 학습 설정을 맞춘다. 이걸 놓치면 학습이 시작 즉시 중단된다.

---

## 5. 데이터 계획

### 5.1 기능별 데이터 확보 경로

| 기능 | 데이터 | 경로 | 현재 |
|---|---|---|---|
| **F9** | 한국어 function calling | 공개 데이터셋 | ✅ 가용 |
| **P1 (F1 F6 F11a F13)** | 강의계획서 + 정답 스키마 | 자체 구축 (증류) | ⏳ 수집 중 |
| F4 요약 | 한국어 문서요약 | 공공 데이터 플랫폼 | ⏳ 승인 신청 |
| 문서 파싱 | 한국어 OCR | 공공 데이터 플랫폼 | ⏳ 승인 신청 |
| F12 | 관심사 매칭 | P2 재사용 | 별도 불필요 |

### 5.2 P1 학습 데이터 구축 절차

공개 데이터가 없으므로 직접 만든다. **이 과정 자체가 프로젝트 기여다.**

| 순서 | 작업 | 담당 |
|---|---|---|
| 1 | 강의계획서 수집 | 수집 파트 |
| 2 | 30~50건 **수작업 정답 라벨링** → 평가셋 고정 | 사람 |
| 3 | 나머지 문서를 Teacher로 라벨 생성 | 클라우드 |
| 4 | **자동 필터** 통과분만 채택 | 자동 |
| 5 | 통과분으로 Student 학습 | 클라우드 |
| 6 | 고정 평가셋으로 정확도 측정 | 자동 |

### 5.3 자동 필터 기준 (4단계)

Teacher 출력을 그대로 쓰면 **오류까지 학습된다.** 다음 조건을 모두 통과한 것만 학습셋에 넣는다.

| 조건 | 확인 내용 |
|---|---|
| 스키마 검증 | 필수 필드 존재, 타입 일치 |
| 날짜 유효성 | 실재하는 날짜, 학기 범위 내 |
| **출처 문장 실존성** | 반환된 출처 문장이 원문에 실제로 존재하는가 |
| 중복 없음 | 동일 항목 반복 추출 아님 |

세 번째 조건이 환각을 걸러내는 핵심이다. 원문에 없는 문장을 근거로 든 추출은 신뢰할 수 없다.

### 5.4 평가셋 절대 규칙

| 규칙 | 이유 |
|---|---|
| 학습에 절대 사용 금지 | 사용 시 정확도 측정이 무의미 |
| 프로젝트 기간 내내 **고정** | 중간 변경 시 모델 간 비교 불가 |
| 물리적으로 분리 저장 | 실수로 섞이는 것을 구조적으로 차단 |
| 라벨링은 사람이 수행 | 모델이 만든 정답으로 모델을 평가하면 순환 논리 |

---

## 6. 학습 계획

### 6.1 방식

| 항목 | 방식 |
|---|---|
| 학습 기법 | 어댑터 기반 경량 파인튜닝 (전체 가중치 미갱신) |
| 양자화 | 4비트 로드 |
| 메모리 절감 | 그래디언트 체크포인팅 적용 |
| 학습 단위 | 파이프라인별 어댑터 분리 |

**파이프라인마다 어댑터를 분리한다.** F9용과 P1용을 하나로 합치면 한쪽 개선이 다른 쪽을 망가뜨렸을 때 원인 분리가 안 된다.

### 6.2 세션 제약 대응

무료 환경은 세션 시간과 주간 할당에 제한이 있고, **종료 시 디스크가 초기화된다.**

| 제약 | 대응 |
|---|---|
| 세션 시간 제한 | 중간 체크포인트 주기 저장 → 다음 세션에서 이어서 |
| 주간 할당 | 1회 학습을 할당 내로 설계. 무제한 반복 불가 |
| 디스크 초기화 | **매 세션 종료 전 외부 저장소로 업로드 필수** |
| 저장 용량 | 어댑터만 보존 (수십 MB). 병합 모델은 변환 후 즉시 업로드 |

체크포인트와 업로드를 빠뜨리면 **수 시간 학습이 통째로 소실된다.** 이 두 가지는 선택이 아니다.

### 6.3 환경 간 연결

```
학교 서버 ──수집 데이터──▶ 외부 저장소 ──▶ 클라우드 학습
    ▲                                          │
    └───────── 변환 모델 ◀── 외부 저장소 ◀──────┘
```

두 환경은 직접 통신하지 않는다. **외부 모델 저장소가 유일한 통로**이므로, 여기 올리지 않은 것은 존재하지 않는 것과 같다.

---

## 7. 평가 계획

### 7.1 지표

| 대상 | 지표 | 목표 | 계획서 연결 |
|---|---|---|---|
| P1 | 마감일 추출 정확도 | 90% 이상 | 성과 지표 |
| P1 | 과제명 추출 정확도 | 측정 | |
| P1 | 스키마 검증 통과율 | 측정 | 품질 관리 |
| P2 | 출처 표기율 | 95% 이상 | RAG 근거성 |
| P2 | 정답 청크 검색 포함률 | 측정 | |
| P3 | 도구 선택 정확도 | 측정 | |
| P3 | 인자 채움 정확도 | 측정 | |
| P3 | 응답 지연 | 5초 이내 | 성과 지표 |
| P4 | 브리핑 만족도 | 4.0 / 5 | 성과 지표 |
| F11 | 오매칭률 | 10% 이하 | 성과 지표 |

### 7.2 비교 실험 설계

계획서의 **"모델 비교 실험으로 선정 근거 정량화"** 항목을 다음 3자 비교로 충족한다.

| 비교 대상 | 확인 내용 |
|---|---|
| 학습 전 Base 모델 | 파인튜닝 효과가 실재하는가 |
| 학습 없는 Instruct 모델 | "그냥 기성 모델 쓰면 되지 않나"에 대한 답 |
| **학습 후 우리 모델** | 결과 |

세 결과를 동일 평가셋·동일 조건으로 측정한다. 이 표가 없으면 파인튜닝의 정당성을 증명할 수 없다.

### 7.3 측정 원칙

| 원칙 | 이유 |
|---|---|
| 학습 **전에** 평가셋 분리 | 사후 분리는 이미 오염된 상태 |
| 동일 프롬프트로 비교 | 프롬프트가 다르면 모델 비교가 아님 |
| baseline 항상 함께 측정 | 개선 폭을 알 수 없으면 수치가 무의미 |
| 실패 사례 보존 | 개선 방향의 근거. 성공률만 남기면 다음에 뭘 고칠지 모름 |

---

## 8. 품질 관리

| 항목 | 방침 |
|---|---|
| 프롬프트 버전 관리 | 코드와 함께 버전 관리. **평가셋 정확도가 개선된 경우에만 반영** |
| 환각 억제 | 추출 결과에 원문 근거 동반. RAG 응답에 출처 페이지 표기 |
| 실패 처리 | 신뢰도 낮은 항목은 자동 등록하지 않고 "확인 필요"로 분리 |
| 어댑터 버전 관리 | 학습 데이터 버전 · 하이퍼파라미터 · 평가 결과를 함께 기록 |
| 개인정보 | 식별 정보는 프롬프트에 포함하지 않음. 필요 시 항목 단위 최소화 |

---

## 9. 위험 관리

| 위험 | 영향 | 대응 |
|---|---|---|
| **강의계획서 미확보** | P1 학습·평가 불가 | 수집 파트 최우선 처리. 수강신청 기간 내 확보 |
| 파인튜닝 효과 미미 | 캡스톤 성과 부실 | baseline 비교로 조기 확인. 미미하면 프롬프트·스키마 개선으로 방향 전환 |
| 무료 GPU 할당 부족 | 학습 반복 불가 | 소형 모델 검증 후 본 학습 1회 설계. 시행착오를 소형에서 소진 |
| 학습 결과 소실 | 수 시간 낭비 | 체크포인트 + 외부 업로드 의무화 |
| Teacher 라벨 품질 저하 | 오류 학습 | 4단계 자동 필터. 통과율이 낮으면 Teacher 교체 |
| 평가셋 오염 | 지표 신뢰성 상실 | 물리적 분리 저장, 학습 스크립트가 해당 경로를 읽지 않도록 구조화 |
| 서식 편중 | 일반화 실패 | 수집 시 학과·유형 분산 |
| 팀원 학사 일정 집중기 | 개발 속도 저하 | 중간·기말 기간을 저부하로 편성 |

---

## 10. 산출물

AI 파트의 결과물은 기능 코드가 아니라 다음 항목이다.

| # | 산출물 | 비고 |
|---|---|---|
| 1 | 파이프라인 4종 (P1~P4) | 구조화 추출 · 검색 · 대화 · 생성 |
| 2 | **학습된 어댑터** | 파이프라인별 분리 |
| 3 | **자체 구축 학습 데이터셋** | 공개 데이터가 없어 직접 구축 |
| 4 | **고정 평가셋 + 라벨** | 정확도 측정 기준 |
| 5 | 출력 스키마 정의서 | 백엔드와의 계약 |
| 6 | 프롬프트 세트 + 버전 이력 | |
| 7 | **비교 실험 결과 보고서** | Base vs Instruct vs 학습 후 |
| 8 | 신뢰도 산출 규칙 | 게이트 분기 기준 |

3·4번이 이 프로젝트의 고유 기여다. **한국어 강의계획서 추출 데이터셋은 공개된 것이 없으므로, 구축 자체가 결과보고서에 쓸 성과다.**

---

## 11. 즉시 착수 항목

| 우선순위 | 작업 | 마감 |
|---|---|---|
| 1 | **강의계획서 확보** (수집 파트) | 수강신청 종료 전 |
| 2 | 공공 데이터 플랫폼 다운로드 승인 신청 | 오늘 (승인 수일 소요) |
| 3 | 학습 환경 계정 · 인증 | 2일 내 |
| 4 | 최소 모델 학습 루프 검증 | 5일 내 |
| 5 | 추출 스키마 초안 확정 | 1주 내 |

1·2번은 **외부 일정에 묶여 있어 미루면 되돌릴 수 없다.** 3~5번은 팀 내부 일정이므로 조정 가능하다.
