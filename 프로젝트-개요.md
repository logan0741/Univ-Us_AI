# Univ-Us AI — 프로젝트 전체 개요

> 이 저장소가 무엇이고, 무엇을 만들며, **데이터 수집은 왜 로컬에서 돌려야 하는지**를
> 한 문서로 설명합니다. 처음 합류했거나, 로컬에서 수집을 돌릴 사람이 먼저 보는 문서입니다.

---

## 1. 이게 무슨 프로젝트인가

인공지능혁신융합사업단 WE-Meet **Output** 팀의 **Univ-Us(캠퍼스메이트)** 중 **AI 에이전트 파트**입니다.
대학생의 학사 정보(강의계획서·공지·과제·시간표)를 **자동으로 모아 → 이해 → 일정으로 만들고 → 먼저 알려주는** 개인 비서입니다.

이 저장소는 **"입력을 주면 검증된 결과를 돌려주는 AI 모듈"** 까지 책임집니다.
화면(프론트)과 REST API(백엔드)는 다른 저장소입니다.

### 문서 지도 (자세한 건 각 문서로)

| 문서 | 무엇 |
|---|---|
| [README](README.md) | 규칙 — 브랜치·커밋·이슈·환경 설치 |
| [Univ-Us_기능명세서.md](Univ-Us_기능명세서.md) | **무엇을** 만드나 — F1~F17, 데이터 흐름, 인수 기준 |
| [Univ-Us_역할별_기능분해서.md](Univ-Us_역할별_기능분해서.md) | **누가** 만드나 — AI/BE/FE 경계 |
| [docs/AI_MODULES.md](docs/AI_MODULES.md) | AI 저장소는 **어떻게** — 9개 기능 MVP |
| [docs/DEV_PLAN.md](docs/DEV_PLAN.md) | **지금 뭐 하나** — 1차 계획·역할·칸반 |
| [수집_파이프라인_계획서.md](수집_파이프라인_계획서.md) | 데이터 **수집** 설계 |
| [AI_에이전트_개발계획서.md](AI_에이전트_개발계획서.md) | AI **학습·파이프라인** 설계 |
| [docs/크롤링-사용법.md](docs/크롤링-사용법.md) | 수집 도구 **사용법** (상세) |
| [docs/GPU-환경과-모델-선정.md](docs/GPU-환경과-모델-선정.md) | GPU 환경·모델 후보 |

---

## 2. 무엇을 만드나

17개 기능(F1~F17)을 **9개 AI 기능**으로 재편했고, AI 파트는 이를 **파이프라인 4종**으로 묶어 개발합니다.

| 파이프라인 | 하는 일 | 담당 기능 |
|---|---|---|
| **P1 구조화 추출** | 비정형 문서 → 고정 스키마 + 출처 문장 | F1·F6·F11a·F13 |
| **P2 임베딩·검색** | 청크 → 색인 → 유사 검색 | F4·F12 |
| **P3 대화 오케스트레이션** | 자연어 → 도구 호출 → 자연어 응답 | F9 |
| **P4 자연어 생성** | 구조화 데이터 → 문장(브리핑) | F10·F11b |

**P1 이 전체의 절반 이상**입니다. 강의계획서에서 과제·마감을 뽑는 게 핵심이고, 여기가 되어야 나머지가 쓸 데이터가 생깁니다.

### 1차 목표 (~2026-09-14)

| 담당 | 맡은 일 | 마일스톤 |
|---|---|---|
| **윤지욱** | OCR(`pic`) + OCR 결과 텍스트 요약 | 1차 8/24 → 2차 9/07 → 3차 9/14 |
| **정수빈** | 데이터 구글링 (AI 사용 금지) | — |
| **김건희** | LLM 계층 `core` → `doc` → `chat` | core(1차) · doc(2차) · chat(3차) |

상세 일정·이슈는 [칸반 보드](https://github.com/users/logan0741/projects/4)와 [DEV_PLAN](docs/DEV_PLAN.md).

---

## 3. 서버 환경 (학교 GPU 서버)

| 항목 | 값 |
|---|---|
| GPU | A100 80GB **MIG `1g.10gb`** → **실제 쓸 수 있는 VRAM 9.5GiB** |
| PyTorch | 2.13.0+cu129 · venv `~/.venv/univus-ai` |
| ⚠️ venv | 컨테이너 overlay 라 **pod 재시작 시 사라짐** → `bash scripts/setup-gpu-env.sh` 로 복구 |
| 접속·세팅 | [SSH 접속 가이드](docs/SSH-서버-접속-가이드.md) · [작업환경 설정](docs/서버-작업환경-설정.md) |

**80GB 가 아니라 9.5GiB 기준**으로 모델·배치를 잡습니다. 큰 모델 풀 파인튜닝 불가, LoRA·소형 모델 사용.
자세한 모델 후보는 [GPU-환경과-모델-선정.md](docs/GPU-환경과-모델-선정.md).

---

## 4. ⚠️ 데이터 수집은 서버가 아니라 로컬에서 돌립니다

수집 계획서는 "서버에서 실행"을 지향하지만, **실측 결과 이 서버에서는 로그인 단계가 안 됩니다.**

| 확인한 것 | 결과 |
|---|---|
| 서버 → JNU 사이트 HTTPS | ✅ 도달됨 (sugang·hakstd·sel, ~90ms) |
| 서버에서 `login` (브라우저 창) | ❌ **불가** — 헤드리스라 화면 없음(`DISPLAY` 없음, `xvfb` 없음) |
| SSO 세션 IP 바인딩 | ❓ 미확인 — 로컬 세션이 서버 IP 에서 거부될 수 있음 |

그래서 **로그인·수집은 크롬이 있는 로컬 PC에서** 돌리고, 결과물만 서버로 옮기는 방식이 현실적입니다.

```
[로컬 PC] 크롬 로그인 → collect → data/raw, data/sessions
     │  scp (SSH)
     ▼
[학교 서버] data/ 에 원문 배치 → AI 파이프라인이 사용
```

### 로컬에서 수집 돌리기 (요약)

```bash
# 로컬 PC (크롬 있는 곳)
git clone git@github.com:logan0741/Univ-Us_AI.git
cd Univ-Us_AI
git switch collect_ai-agent_logan0741           # 수집 코드가 있는 브랜치

pip install -r requirements-collect.txt
playwright install chromium
cp .env.example .env

python -m src.collect.cli login                 # 브라우저 떠서 직접 로그인 (비밀번호 저장 안 함)
python -m src.collect.cli collect syllabus      # 원문 저장
python -m src.collect.cli status
```

상세(소스별 명령, eClass `.ics`, 문제 해결)는 [docs/크롤링-사용법.md](docs/크롤링-사용법.md).

### 수집 결과를 서버로 옮기기

```bash
# 로컬에서 (SSH/scp 로. 서버 SSH 정보는 SSH 접속 가이드 참고)
scp -P <포트> -r data/raw jovyan@<서버>:~/Univ-Us_AI/data/
```

> `data/` 와 세션 파일은 **본인 학사 데이터·인증**이라 gitignore 되어 있습니다. git 이 아니라 scp 로 옮깁니다.

---

## 5. 외부 통신 (서버에서 나가는 방향)

로컬↔서버 데이터 이동에 쓸 수 있는 통로입니다. **세 가지 다 됩니다** (이 세션에서 실측).

| 프로토콜 | 포트 | 상태 | 용도 |
|---|---|---|---|
| **SSH** | 22 | ✅ | **scp 로 데이터·세션 이동 (권장)** · git push/pull |
| HTTP | 80 | ✅ | 일반 다운로드 |
| HTTPS | 443 | ✅ | pip·git·API·JNU 사이트 |

프록시·TLS 가로채기(MITM) 없음. SSH 는 이미 GitHub 키가 있어 인증이 해결돼 있습니다.

> 표준 라이브러리 `urllib`/`ssl` 로 HTTPS 호출 시 `CERTIFICATE_VERIFY_FAILED` 가 나면
> `export SSL_CERT_FILE=/etc/ssl/certs/ca-certificates.crt` ([README 9장](README.md)).

---

## 6. 지금 상태

| 부분 | 상태 |
|---|---|
| 수집 시스템 뼈대 (`src/collect`) | ✅ 완료·검증 (ruff·pytest 4건·실브라우저로 로그인 리다이렉트 감지) |
| 실제 로그인해서 내 데이터 수집 | ⏳ **미확인** — 로컬 로그인 필요 (제가 대신 못 함) |
| 소스별 필드 추출기(파서) | ⏳ 미구현 — 로그인 후 실제 HTML 표본 보고 붙임 |
| 문서 | ✅ 개요·사용법·계획서 정리됨 |
| 브랜치 | `collect_ai-agent_logan0741` 에서 작업 중. **main 미변경** |

### 다음 할 일

1. 로컬에서 `login` → `collect` 한 번 성공시켜 **세션이 실제로 먹히는지**(IP 바인딩 여부) 확인
2. 나온 `data/raw/` 원문을 보고 소스별 파서 구현 (P1 로 연결)
3. eClass `.ics` URL 설정해 과제·마감 수집
