<!--
이 파일을 복사해서 쓰세요.

  cp docs/plans/_TEMPLATE.md docs/plans/<기능약어>_<영문이름>.md
  예: cp docs/plans/_TEMPLATE.md docs/plans/pic_jiwook.md

이 원본(_TEMPLATE.md)은 고치지 마세요.

작성 요령
  · 정하지 못한 건 "미정" 이라고 쓰세요. 빈칸으로 두지 마세요.
    "아직 안 정했다" 도 정보입니다. 빈칸은 빠뜨린 건지 없는 건지 알 수 없습니다.
  · 잘 쓰는 게 목적이 아닙니다. 일주일 뒤의 내가 읽고 이어갈 수 있으면 됩니다.
  · 진행하면서 바뀌면 고쳐서 다시 올리세요. 처음 쓴 대로 지킬 필요 없습니다.
    다만 "왜 바꿨는지" 한 줄은 12장에 남기세요.
-->

# <기능약어>_ai-agent 세부 계획 — <이름>

| 항목 | 내용 |
| --- | --- |
| 담당 | <이름> |
| 기능 메인 브랜치 | `<기능약어>_ai-agent` |
| 내 작업 브랜치 | `<기능약어>_ai-agent_<영문이름>` |
| 대응 기능 | <!-- 예: F1·F6 의 이미지 입력 경로 · DF-1 --> |
| 최초 작성 | 2026-__-__ |
| 마지막 갱신 | 2026-__-__ |

---

## 1. 1차에 무엇을 만드나

<!-- 9/14 에 "이게 됩니다" 하고 보여줄 것을 한 문단으로.
     기능 이름 말고 동작으로 쓰세요.
     ❌ "OCR 기능 개발"
     ✅ "시간표 사진을 넣으면 과목명·요일·시간이 담긴 JSON이 나온다" -->

## 2. 입력 / 출력

| | 내용 |
| --- | --- |
| 입력 | <!-- 형식과 예시. 예: 시간표 사진 1장 (JPG/PNG, 1024px 이상) --> |
| 출력 | <!-- 스키마. 예: {과목명, 요일, 시작, 종료, 강의실} 리스트 --> |

<!-- 출력 예시를 실제 JSON으로 하나 적어두면 나중에 아주 편합니다 -->

```json
{
  "예시": "여기에 실제 출력 형태를 적으세요"
}
```

## 3. 완료 기준

<!-- docs/AI_MODULES.md 의 내 기능 "완료 기준" 을 먼저 보고 가져오세요.
     "구현했다" 가 아니라 "이렇게 하면 동작을 보여줄 수 있다" 로 씁니다.
     체크박스는 진행하면서 채웁니다. -->

- [ ]
- [ ]
- [ ]
- [ ]

## 4. 쓸 기술과 그렇게 정한 이유

| 항목 | 선택 | 왜 이걸 골랐나 |
| --- | --- | --- |
| 모델 / 라이브러리 | | |
| 모델 슬롯 | <!-- LLM_MAIN / DOC_PARSER / VISION / EMBEDDING / RULE --> | |
| 주요 패키지 | | |

<!-- "왜" 칸이 이 표의 핵심입니다.
     나중에 안 될 때, 무엇을 바꿔봐야 하는지 여기서 나옵니다.
     "다들 이거 쓰길래" 도 이유입니다. 그렇게 쓰세요. -->

### 검토했지만 안 쓴 것

<!-- 있으면 적으세요. 없으면 "없음".
     같은 걸 다른 사람이 다시 검토하는 시간을 아낍니다. -->

## 5. GPU가 필요한가

- [ ] 필요 없음 → 아래는 건너뛰세요
- [ ] 필요함

| 항목 | 내용 |
| --- | --- |
| 쓰는 이유 | <!-- 예: 로컬 OCR 모델 파인튜닝 --> |
| 필요 VRAM (예상) | <!-- 실제로 확인한 값. nvitop -1 로 할당량 먼저 보세요 --> |
| 할당된 VRAM | <!-- MIG 로 쪼개져 있으면 카드 전체가 아닙니다 --> |
| 안 맞으면 어떻게 | <!-- 예: LoRA 로 전환 / 모델 크기 축소 / API 경로 --> |

## 6. 만드는 순서

<!-- 2~3일 단위로 자르세요. 이 목록이 그대로 GitHub Issue 가 됩니다.
     일주일 넘게 걸릴 것 같은 항목은 더 쪼개세요. -->

| # | 할 일 | 예상 | 이슈 |
| :---: | --- | --- | --- |
| 1 | | __일 | #__ |
| 2 | | __일 | #__ |
| 3 | | __일 | #__ |
| 4 | | __일 | #__ |

## 7. 다른 사람에게 받아야 하는 것

<!-- 없으면 "없음". 있으면 언제까지 필요한지 꼭 적으세요.
     못 받으면 그만큼 늦어진다는 걸 미리 알려야 합니다. -->

| 무엇을 | 누구에게 | 언제까지 | 없으면 |
| --- | --- | --- | --- |
| | | | <!-- 그동안 뭘 하고 있을지 --> |

## 8. 내가 만들어서 남에게 주는 것

<!-- 내 출력을 누가 받아 쓰는지. 스키마를 바꾸면 그 사람 코드가 조용히 깨집니다. -->

| 무엇을 | 누구에게 | 언제쯤 |
| --- | --- | --- |
| | | |

## 9. 공용 코드를 건드리나

- [ ] `src/core/` 수정 필요
- [ ] `src/schemas/` 수정 필요
- [ ] 없음

> 위 두 개를 건드려야 하면 **이슈로 먼저 공유하고 합의한 뒤에** 고칩니다.
> 여러 기능이 같이 쓰는 코드라, 말없이 바꾸면 다른 사람 코드가 조용히 깨집니다.

## 10. 어떻게 확인하나

<!-- 다 만들었을 때 "됐다" 를 무엇으로 판정할지.
     명령어와 기대 출력을 적어두면 나중에 그대로 데모가 됩니다. -->

```bash
# 실행 명령

# 기대 출력
```

| 확인 항목 | 목표 | 어떻게 잴 것인가 |
| --- | --- | --- |
| | | |

## 11. 안 되면 어떻게 할 것인가

<!-- 미리 정해두면 급할 때 판단이 빨라집니다. -->

| 예상되는 문제 | 대안 |
| --- | --- |
| | |

## 12. 진행 기록

<!-- 계획이 바뀌면 여기에 한 줄씩. 최신이 위로.
     "왜 바꿨는지" 가 중요합니다. -->

| 날짜 | 바뀐 것 | 왜 |
| --- | --- | --- |
| 2026-__-__ | 최초 작성 | — |
