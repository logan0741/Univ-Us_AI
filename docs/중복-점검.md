# 중복 저장 점검

> `data/` 스캔 자동 생성. 최종 갱신: 2026-09-01 15:50

재실행: `python -m src.collect.cli dedup` (격리까지: `--fix`)

---

## 요약

| 항목 | 값 |
| --- | ---: |
| 파일 중복 그룹 | 1 |
| 여분 파일 | 7 |
| 낭비 용량 | 0.0MB |
| 내용 중복 그룹(게시판·카테고리 간) | 0 |
| 이번에 격리한 파일 | 0 |

판별 기준: 파일=바이트 sha256, 내용=제목+본문 정규화 sha256(정확 일치).
표현만 다른 유사 중복은 추후 LLM 임베딩으로 별도 처리.

---

## 파일 중복 (같은 바이트가 여러 곳에)

- `e3b0c44298fc` × 8 — raw\notice_aicoss\20260901-154909__https · raw\notice_aicoss\20260901-154912__https · raw\notice_aisw\20260901-154902__https · raw\notice_aisw\20260901-154905__https

## 내용 중복 (게시판·카테고리 넘나든 같은 글)

없음.

---

## 앞으로 중복이 안 쌓이게
- 다운로드(이미지·PDF·첨부)는 `download` 가 바이트 해시로 같은 파일을 재저장하지 않습니다.
- 공지 상세는 게시판이 달라도 같은 글이면 저장하지 않습니다(내용키).
- 위 두 방지는 이 점검이 만든 레지스트리(`data/index/_*_.jsonl`)를 참조합니다.
