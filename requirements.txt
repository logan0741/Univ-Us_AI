# ============================================================
# Univ-Us_AI — 공통 패키지 (전원 필수)
#
#   conda activate univus-ai
#   pip install -r requirements.txt -r requirements-dev.txt
#
# GPU 관련 패키지는 requirements-gpu.txt 에 따로 있습니다.
# 이 파일만 설치해도 대부분의 기능을 개발할 수 있습니다.
#
# 버전 표기 규칙: >=A,<B
#   하한(>=)  팀원끼리 같은 세대를 쓰게 맞춥니다
#   상한(<)   프로젝트 중간에 메이저 버전이 올라가 코드가 깨지는 것을 막습니다
#   ※ 상한을 임의로 올리지 마세요. 올려야 하면 Issue 로 먼저 공유합니다.
# ============================================================

# ── 설정 / 스키마 검증 ───────────────────────────────────────
# LLM이 뱉은 JSON을 여기서 검증합니다 (계획서 4.4 다)
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
python-dotenv>=1.0,<2
pyyaml>=6.0,<7

# ── HTTP / 재시도 ────────────────────────────────────────────
httpx>=0.28,<1
requests>=2.32,<3
tenacity>=9.0,<10          # API 호출 실패 시 재시도

# ── LLM 호출 ────────────────────────────────────────────────
# 계획서 4.3 나: "LLM 호출부는 OpenAI 호환 인터페이스로 추상화"
# → 모델이 무엇으로 정해지든 이 SDK로 호출합니다 (base_url 만 교체)
openai>=2.0,<3

# ── LLM 오케스트레이션 / RAG ─────────────────────────────────
# 인터넷의 langchain 0.x 예제는 그대로 동작하지 않습니다.
# 반드시 1.x 공식 문서를 보세요: https://python.langchain.com
langchain>=1.0,<2
langchain-core>=1.0,<2
langchain-text-splitters>=1.0,<2

# ── 문서 처리 ───────────────────────────────────────────────
pypdf>=6.0,<7              # PDF 텍스트 추출
python-docx>=1.2,<2        # docx
pillow>=11.0,<13           # 이미지
beautifulsoup4>=4.13,<5    # 공지 게시판 HTML 파싱
lxml>=6.0,<7

# ── 날짜 / 데이터 ───────────────────────────────────────────
python-dateutil>=2.9,<3    # "6/12(목) 23:59" 같은 마감일 파싱
pandas>=3.0,<4
numpy>=2.1,<3

# ── 평가 지표 (eval_ai-agent) ───────────────────────────────
scikit-learn>=1.6,<2

# ── DB / 벡터 검색 ──────────────────────────────────────────
sqlalchemy>=2.0,<3
psycopg[binary]>=3.2,<4
pgvector>=0.4,<1
alembic>=1.14,<2

# ── 스케줄러 (brief_ai-agent, notice_ai-agent) ──────────────
apscheduler>=3.11,<4

# ── 로컬 실행 / 데모용 API ──────────────────────────────────
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1

# ── CLI / 로그 ──────────────────────────────────────────────
typer>=0.15,<1
rich>=14.0,<16
structlog>=25.0,<27
tqdm>=4.67,<5


# ============================================================
# 모델 프로바이더 SDK — 사용 모델 확정 후 주석 해제
#
# 어떤 모델을 쓸지는 eval_ai-agent 의 비교 실험으로 정합니다.
# 확정되기 전까지는 위의 openai SDK + base_url 로 호출합니다.
# 임의로 주석을 풀지 말고, 확정되면 팀 합의 후 함께 해제합니다.
# ============================================================
# langchain-upstage
# anthropic
# google-genai
