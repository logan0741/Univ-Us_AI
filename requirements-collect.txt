# ============================================================
# Univ-Us_AI — 데이터 수집 전용 패키지
#
#            ⚠️  수집 담당만 설치하세요.
#
# 언제 필요한가
#   · JNU 학사 페이지(로그인 필요)를 본인 세션으로 수집할 때
#     (강의계획서 / 시간표 / 수강내역 / eClass)
#
# 설치
#   pip install -r requirements-collect.txt
#   playwright install chromium      ← 브라우저 실물 다운로드 (~150MB, 필수)
#
# 나머지 수집 도구(httpx·beautifulsoup4·lxml·apscheduler·typer·
# structlog·tenacity)는 requirements.txt 에 이미 있습니다.
# ============================================================

# ── 브라우저 자동화 (SSO 로그인 · JS 페이지) ────────────────
#
# 학사 페이지는 로그인(SSO)과 자바스크립트로 그려집니다.
# requests/httpx 만으로는 로그인 세션을 못 넘기므로 실제 브라우저가 필요합니다.
#
# 비밀번호는 저장하지 않습니다. 브라우저에서 직접 로그인하고
# 그 결과 세션(쿠키)만 재사용합니다.
playwright>=1.40,<2

# ── iCalendar 파싱 (eClass = Moodle 과제·마감) ──────────────
# eClass 는 Moodle 이라 캘린더를 .ics 로 내보낼 수 있습니다.
# 게시판을 긁는 것보다 안정적입니다.
icalendar>=6.0,<7
