# ============================================================
# Univ-Us_AI — 개발 도구 (전원 권장)
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# 배포 환경에는 설치하지 않습니다.
# ============================================================

# ── 테스트 ──────────────────────────────────────────────────
pytest>=8.4,<10
pytest-asyncio>=1.0,<2
pytest-cov>=6.0,<8

# ── 정적 분석 / 포맷 ────────────────────────────────────────
# PR 올리기 전에 반드시: ruff check .
ruff>=0.14,<1
mypy>=1.15,<3

# ── 커밋 전 자동 검사 ───────────────────────────────────────
pre-commit>=4.0,<5

# ── 노트북 실험 ─────────────────────────────────────────────
# 실험은 노트북에서 하되, 확정된 코드는 src/ 로 옮깁니다.
# 노트북은 diff 가 읽히지 않아 리뷰가 불가능합니다.
ipykernel>=6.29,<8
jupyterlab>=4.4,<5

# ── 타입 스텁 ───────────────────────────────────────────────
types-requests
types-python-dateutil
types-PyYAML
